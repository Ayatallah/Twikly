<p>
	<strong>Commenter:</strong>
	<%= @comment.commenter %>
</p>
<p>
	<strong>Text:</strong>
	<%= @comment.body %>
</p>
<h2>Sub Comments</h2>
<%= render @comment.sub_comments %>

<h2>Add a sub comment:</h2>
<%= render 'sub_comments/form' %>

<%= link_to 'Back', article_path(@comment.article) %>
